/**
 * Token secreto para la URL del portal de informes.
 * Ejemplo de URL: /informes/TU_TOKEN_AQUI
 *
 * Cambia este valor por una cadena larga y aleatoria antes de compartir el enlace.
 * La URL en sí actúa como “contraseña débil”; para mayor seguridad usa autenticación en servidor.
 */
export const reportsPortalSecret = 's3cr3t';
